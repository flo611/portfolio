const Main = ({ children }) => {
  return (
    <main className="bg-cyan-400 text-white dark:bg-black dark:text-white dark:uppercase lg:text-2xl">
      {children}
    </main>
  );
};

export default Main;
